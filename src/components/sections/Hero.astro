---
import EmailButtons from '../EmailButtons.astro'
import Fireflies from '../animations/Fireflies.astro'
---

<section id="home" class="hero-background">
  <Fireflies />
  <main class="hero-container">
    <div class="hero-content-grid">
      <picture class="hero-photo-container-grid">
        <img src="./perfil.png" alt="Foto de perfil de Miqueas Moreno" />
      </picture>
      <h2>¡Hola, soy Miqueas!</h2>
      <h1>Creo experiencias digitales únicas</h1>
        <EmailButtons />
        <aside class="hero-paragraph-container">
          <p>
            <b>Desarrollador web comprometido</b> no solo en efectuar proyectos
            sino en <b>impulsar tu éxito</b>
          </p>
        </aside>

    </div>
  </main>
</section>

<style>

  /* HERO STYLES */

  .hero-background {
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100svh;
    min-height: 100vh;
  }

  .hero-container {
    position: relative;
    margin-inline: 1rem;
  }

  .hero-actions {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 12px;
  }

  .hero-email-container {
    display: flex;
    gap: 12px;
    background-color: blue;
  }
  .hero-email-container button {
    font-family: Montserrat, system-ui, sans-serif;
    font-weight: 600;
    cursor: pointer;
    background-color: var(--secondary-d);
    color: var(--primary-l);
  }

  .hero-email-text {
    border-radius: 80px 3px;
    padding-block: 6px;
    padding-inline: 24px;
  }

  .hero-email-buttons-container {
    display: flex;
    gap: 8px;
  }

  .hero-email-button {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 36px;
    border-radius: 10px;
    padding: 4px;
  }
  .hero-email-button svg {
    width: 16px;
    stroke: var(--primary-l);
  }

  .hero-paragraph-container {
    text-align: center;
  }

  .hero-paragraph-container p {
    max-width: 29ch;
    font-size: .9rem;
    text-wrap: pretty;
    margin-inline: auto;
  }

  .hero-paragraph-container p b {
    color: var(--secondary-d);
  }


  /* Testing GRID */

  .hero-content-grid {
    display: grid;
    column-gap: 1rem;
    row-gap: 1rem;
  }

  .hero-photo-container-grid {
    display: flex;
    grid-row: 2;
    margin-bottom: -1rem;
    justify-self: center
  }

  .hero-photo-container-grid img {
    max-width: 100%;
    width: 230px;
  }

  .hero-content-grid h2 {
    font-size: 1.25rem;
    color: var(--secondary-d);
    border: 2px solid var(--secondary-d);
    border-radius: 80px 3px;
    padding-block: 6px;
    grid-column: 1 / 2;
    grid-row: 1 / 3;
    align-self: end;
    width: 100%;
    text-align: center;
  }

  .hero-content-grid h1 {
    font-size: 1.56rem;
    color: var(--secondary-l);
    border: 2px solid var(--secondary-l);
    border-radius: 3px 3px 80px 80px;
    padding-block: 6px;
    grid-column: 1 / 3;
    text-wrap: balance;
    width: 100%;
    text-align: center;
  }

  .hero-titles-grid {
    display: flex;
    align-items: flex-start;
    flex-direction: column;
    gap: 12px;
    margin-bottom: 16px;
    grid-column: 1 / 2;
    grid-row: 2 / 3
  }

  .hero-email-container {
    flex-direction: column;
    align-self: center;
    align-items: center;
  }

  .hero-content-grid h2 {
      font-size: 1.2rem;
    }
    .hero-content-grid h1 {
      font-size: 1.5rem;
    }

  @media (max-width: 750px) {
    .hero-content-grid h2 {
      font-size: 1rem;
    }
    .hero-content-grid h1 {
      font-size: 1.3rem;
    }
    .hero-photo-container-grid img {
      width: 200px; 
    }
    .hero-email-container {
      flex-direction: column;
      align-self: center;
      align-items: center;
    }
    .hero-email-text {
      font-size: 0.7rem;
    }
    .hero-email-button {
      width: 40px;
      height: 26px;
    }
    .hero-paragraph-container p {
      font-size: .8rem;
    }
  }

  @media (max-width: 675px) {
    .hero-content-grid h2 {
      font-size: 0.9rem;
    }
    .hero-content-grid h1 {
      font-size: 1.2rem;
    }
    .hero-photo-container-grid img {
      width: 150px; 
    }
    .hero-email-text {
      font-size: 0.55rem;
    }
    .hero-email-button {
      width: 40px;
      height: 26px;
    }
    .hero-paragraph-container p {
      font-size: .75rem;
    }
    
  }

  @media (max-width: 600px) {
    .hero-content-grid h2 {
      font-size: 0.9rem;
    }
    .hero-content-grid h1 {
      font-size: 1.1rem;
    }
    .hero-photo-container-grid img {
      width: 130px; 
    }
    .hero-email-text {
      font-size: 0.5rem;
    }
    .hero-email-button {
      width: 40px;
      height: 26px;
    }
    .hero-paragraph-container p {
      font-size: .69rem;
    }
  }

  @media (max-width: 480px) {
    .hero-content-grid h2 {
      font-size: 0.8rem;
    }
    .hero-content-grid h1 {
      font-size: 1.33rem;
    }
    .hero-email-text {
      font-size: 0.5rem;
    }
    .hero-email-button {
      width: 36px;
      height: 26px;
    }
    .hero-paragraph-container p {
      font-size: .60rem;
    }
  }

</style>
